<?php 
$pageTitle = 'Home';
$contentView = 'views/home_content.php';
include 'views/layouts/main_layout.php';
?>

<?php // This is the actual content that will be included in the main layout ?>
<div class="row">
    <!-- Left Sidebar -->
    <div class="col-lg-3 mb-4">
        <div class="card shadow-sm">
            <div class="card-body">
                <div class="d-flex align-items-center mb-3">
                    <?php if (isset($currentUser['profile_picture']) && $currentUser['profile_picture'] != 'default.jpg'): ?>
                        <img src="public/uploads/profile_pictures/<?php echo $currentUser['profile_picture']; ?>" class="rounded-circle me-3" width="60" height="60" alt="Profile">
                    <?php else: ?>
                        <div class="bg-primary text-white rounded-circle me-3 d-flex align-items-center justify-content-center" style="width: 60px; height: 60px;">
                            <i class="fas fa-user fa-2x"></i>
                        </div>
                    <?php endif; ?>
                    <div>
                        <h5 class="card-title mb-0"><?php echo $currentUser['first_name'] . ' ' . $currentUser['last_name']; ?></h5>
                        <p class="text-muted small mb-0">@<?php echo $currentUser['username']; ?></p>
                    </div>
                </div>
                <div class="list-group list-group-flush">
                    <a href="index.php" class="list-group-item list-group-item-action active">
                        <i class="fas fa-newspaper me-2"></i> News Feed
                    </a>
                    <a href="index.php?page=profile" class="list-group-item list-group-item-action">
                        <i class="fas fa-user me-2"></i> My Profile
                    </a>
                    <a href="index.php?page=profile&action=edit" class="list-group-item list-group-item-action">
                        <i class="fas fa-cog me-2"></i> Edit Profile
                    </a>
                    <a href="index.php?page=auth&action=logout" class="list-group-item list-group-item-action text-danger">
                        <i class="fas fa-sign-out-alt me-2"></i> Logout
                    </a>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Main Content Area -->
    <div class="col-lg-6 mb-4">
        <!-- Create Post Card -->
        <div class="card shadow-sm mb-4">
            <div class="card-body">
                <h5 class="card-title">Create a Post</h5>
                <form action="index.php?page=post&action=create" method="post">
                    <div class="mb-3">
                        <textarea class="form-control" id="content" name="content" rows="3" placeholder="What's on your mind, <?php echo $currentUser['first_name']; ?>?" required></textarea>
                    </div>
                    <div class="text-end">
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-paper-plane me-1"></i> Post
                        </button>
                    </div>
                </form>
            </div>
        </div>
        
        <!-- Posts Feed -->
        <?php if (empty($posts)): ?>
            <div class="alert alert-info">
                <i class="fas fa-info-circle me-2"></i> No posts yet. Be the first to post something!
            </div>
        <?php else: ?>
            <?php foreach ($posts as $post): ?>
                <div class="card shadow-sm mb-4">
                    <div class="card-header bg-white">
                        <div class="d-flex justify-content-between align-items-center">
                            <div class="d-flex align-items-center">
                                <?php if (isset($post['profile_picture']) && $post['profile_picture'] != 'default.jpg'): ?>
                                    <img src="public/uploads/profile_pictures/<?php echo $post['profile_picture']; ?>" class="rounded-circle me-2" width="40" height="40" alt="Profile">
                                <?php else: ?>
                                    <div class="bg-primary text-white rounded-circle me-2 d-flex align-items-center justify-content-center" style="width: 40px; height: 40px;">
                                        <i class="fas fa-user"></i>
                                    </div>
                                <?php endif; ?>
                                <div>
                                    <h6 class="mb-0"><?php echo $post['first_name'] . ' ' . $post['last_name']; ?></h6>
                                    <p class="text-muted small mb-0"><?php echo formatDateTime($post['created_at']); ?></p>
                                </div>
                            </div>
                            <?php if ($post['user_id'] == $currentUser['user_id']): ?>
                                <div class="dropdown">
                                    <button class="btn btn-sm btn-light" type="button" data-bs-toggle="dropdown">
                                        <i class="fas fa-ellipsis-v"></i>
                                    </button>
                                    <div class="dropdown-menu dropdown-menu-end">
                                        <form action="index.php?page=post&action=delete" method="post">
                                            <input type="hidden" name="post_id" value="<?php echo $post['post_id']; ?>">
                                            <button type="submit" class="dropdown-item text-danger" onclick="return confirm('Are you sure you want to delete this post?')">
                                                <i class="fas fa-trash-alt me-2"></i> Delete
                                            </button>
                                        </form>
                                    </div>
                                </div>
                            <?php endif; ?>
                        </div>
                    </div>
                    <div class="card-body">
                        <p class="card-text"><?php echo nl2br(htmlspecialchars($post['content'])); ?></p>
                    </div>
                    <div class="card-footer bg-white">
                        <div class="d-flex justify-content-between align-items-center">
                            <a href="index.php?page=post&action=view&id=<?php echo $post['post_id']; ?>" class="text-decoration-none">
                                <i class="far fa-comment me-1"></i> 
                                <?php echo $post['comment_count']; ?> Comment<?php echo $post['comment_count'] != 1 ? 's' : ''; ?>
                            </a>
                            <a href="index.php?page=post&action=view&id=<?php echo $post['post_id']; ?>" class="btn btn-sm btn-outline-primary">
                                View Post
                            </a>
                        </div>
                    </div>
                </div>
            <?php endforeach; ?>
        <?php endif; ?>
    </div>
    
    <!-- Right Sidebar -->
    <div class="col-lg-3">
        <div class="card shadow-sm mb-4">
            <div class="card-body">
                <h5 class="card-title">Welcome to SocialConnect!</h5>
                <p>Connect with friends and share what matters to you.</p>
                <div class="d-grid">
                    <a href="index.php?page=profile" class="btn btn-outline-primary">
                        <i class="fas fa-user me-1"></i> View My Profile
                    </a>
                </div>
            </div>
        </div>
        
        <div class="card shadow-sm">
            <div class="card-body">
                <h5 class="card-title">Quick Tips</h5>
                <div class="list-group list-group-flush">
                    <div class="list-group-item border-0 ps-0">
                        <i class="fas fa-edit text-primary me-2"></i> Create posts to share your thoughts
                    </div>
                    <div class="list-group-item border-0 ps-0">
                        <i class="fas fa-comment text-primary me-2"></i> Comment on friends' posts
                    </div>
                    <div class="list-group-item border-0 ps-0">
                        <i class="fas fa-user-edit text-primary me-2"></i> Update your profile information
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
